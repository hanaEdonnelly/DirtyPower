<ul class="show-list">
{% assign sorted_members = site.members | sort: "order" | reverse %}
  {% for member in sorted_members %}
    <li class="show-card">
        <h2>{{ member.title }} | {{ member.position }}<br></h2>
        <p>About: {{ member.fact }}</p>
    </li>
  {% endfor %}
</ul>
